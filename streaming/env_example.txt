# OpenAI API Configuration
# Copy this file to .env and add your actual API key

# Your OpenAI API key (get it from https://platform.openai.com/api-keys)
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Customize the model (default is gpt-3.5-turbo)
# OPENAI_MODEL=gpt-4

# Optional: Set a custom timeout (in seconds)
# OPENAI_TIMEOUT=30

# Optional: Set a custom max tokens for responses
# OPENAI_MAX_TOKENS=1000 